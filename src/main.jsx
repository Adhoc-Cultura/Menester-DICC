import React from "react";
import ReactDOM from "react-dom/client";
import MENESTER from "./MENESTER.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MENESTER-DICC />
  </React.StrictMode>
);
